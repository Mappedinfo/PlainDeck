# 复制为 .env.local（或直接在 CI 中设置），真实值不要提交到仓库。
# Vite 只在 .env / .env.local / .env.[mode] 中读取 VITE_* 前缀变量。

# 部署基础路径。GitHub Pages 子路径部署默认 /PlainDeck/；
# 自定义域名或根路径部署时改为 /（注意首尾斜杠）。
VITE_BASE_PATH=/PlainDeck/

# 站点分析（可选）。留空则不注入对应脚本。
VITE_GOOGLE_ANALYTICS_ID=
VITE_BAIDU_ANALYTICS_ID=
